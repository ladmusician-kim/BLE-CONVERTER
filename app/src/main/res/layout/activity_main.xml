<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <ListView
        android:id="@+id/scanningListView"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_alignLeft="@+id/blueswitchText"
        android:layout_alignRight="@+id/blueswitchText"
        android:layout_below="@+id/printData"
        />

    <TextView
        android:id="@+id/blunoText"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:ems="10"
        android:text="Bluno is Disconnected"
        android:textSize="20sp" />
    
    <TextView
        android:id="@+id/blueswitchText"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/blunoText"
        android:layout_below="@+id/blunoText"
        android:layout_marginTop="20dp"
        android:ems="10"
        android:text="BlueSwitch is Disconnected"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/receivedData"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/blueswitchText"
        android:layout_below="@+id/blueswitchText"
        android:layout_marginTop="20dp"
        android:ems="10"
        android:text="Receive data? : "
        android:textSize="20sp" />

    <TextView
        android:id="@+id/printData"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/blueswitchText"
        android:layout_below="@+id/receivedData"
        android:layout_marginTop="20dp"
        android:ems="10"
        android:text="Data: "
        android:textSize="20sp" />
</RelativeLayout>
